{
  "name": "flowtty",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "npm run build -ws --if-present"
  },
  "devDependencies": {
    "typescript": "^6",
    "vitest": "^4"
  }
}
